This script runs nightly, backing up all generated data files to the MPFI network:
\\mpfi.org\Public\Fitzlab\ShrewData

The nightly runs are set from each computer via the Task Scheduler in Windows (Control Panel -> Administrative Tools).

The ARC computers perform backups at 9:30 PM. The imaging computers perform backups at 6:30 AM.

